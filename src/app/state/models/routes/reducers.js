const INITIAL_STATE = {
  isFetching: false,
  routes: []
}

const routesReducer = (state = INITIAL_STATE, action) => {

  switch(action.type) {

    default:
      return state
  }
}

export default routesReducer
